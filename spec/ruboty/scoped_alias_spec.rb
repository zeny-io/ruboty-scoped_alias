require 'spec_helper'

describe Ruboty::ScopedAlias do
  it 'has a version number' do
    expect(Ruboty::ScopedAlias::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
